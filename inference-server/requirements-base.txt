# Base requirements pre-installed in kubeserve-base image
# These are the most common dependencies used by ML models
# Pre-installing reduces cold start time from 45s -> 3s for 90% of models

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0

# Data science essentials (most common in ML models)
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.1
joblib==1.4.2

# Metrics (for Prometheus)
prometheus-fastapi-instrumentator==7.0.0
prometheus-client==0.20.0

# S3 client (for downloading models from Minio)
minio==7.2.4
boto3==1.35.0

